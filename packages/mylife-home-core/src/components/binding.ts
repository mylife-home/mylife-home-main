import { components } from 'mylife-home-common';

export class Binding {
  constructor(registry: components.Registry, sourceId: string, sourceState: string, targetId: string, targetAction: string) {}
}
